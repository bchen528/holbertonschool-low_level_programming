#include "holberton.h"

/**
 * mul - multiplies two integers
 * @a: parameter to be checked
 * @b: parameter to be checked
 * Return: 0
 */

int mul(int a, int b)
{
	int c;
	c = a * b;
	return (c);
}
