#include "holberton.h"

/**
 * print_triangle - prints a triangle
 * @size: parameters to be checked
 */

void print_triangle(int size)
{
	int w, h, b;
	
	for (h = 0; h < size; h++)
	{
		for (w = 0; w < (size + 1); w++)
		{
			_putchar(' ');
			if (
				_putchar('#');

		}
		_putchar('\n');
	}
	if (size <= 0)
		_putchar('\n');
}
