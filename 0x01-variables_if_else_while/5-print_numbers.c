#include <stdio.h>

/**
 * main - the main function
 * Return: 0, exit program
 */

int main(void)
{
int  digit;

for (digit = 0; digit < 10; digit++)
{
printf("%d", digit);
}
putchar('\n');
return (0);
}
